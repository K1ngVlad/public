import { createUseStyles } from 'react-jss';

const TabelFormsStyles = createUseStyles({
  TabelForms: {
    display: 'flex',
    justifyContent: 'space-between',
    width: 920,
    minHeight: 700,
  },
});

export default TabelFormsStyles;
