import { createUseStyles } from 'react-jss';

const TabelHeadStyles = createUseStyles({
  TabelHead: {
    fontSize: 28,
    fontWeight: 500,
    marginBottom: 15,
    fontFamily: 'SF-pro-medium',
  },
});

export default TabelHeadStyles;
